# Use vi editing mode (esc to use) in bash command prompts
# set -o vi

# set the Ctrl-l to clear the screen like in emacs mode
# bind -m vi-insert "\C-l":clear-screen

# set the esc . equivalent for vi command mode
# bind -m vi-command ".":insert-last-argument
